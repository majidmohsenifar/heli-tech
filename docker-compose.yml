version: "3.9"
services:
  user-db:
    image: "postgres"
    environment:
      - POSTGRES_USERNAME=postgres
      - POSTGRES_PASSWORD=postgres
      - POSTGRES_DB=user_db
    restart: on-failure
    ports:
      - "5432:5432"
    volumes:
      - user-db:/var/lib/postgresql/data

volumes:
  user-db:

